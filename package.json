{
  "name": "packagetest",
  "version": "1.0.0",
  "description": "A tet to test packaging apps ",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "if  [ -d \"build\" ]; then rm -rf build/; fi && ./node_modules/.bin/pkg . --out-path build/ -t node8-macos-x64,node8-linux-x64,node8-win-x86,node8-win-x64"
  },
  "author": "MikeModder",
  "license": "GPL-3.0",
  "devDependencies": {
    "pkg": "^4.3.1"
  },
  "dependencies": {
    "express": "^4.16.3",
    "yargs": "^11.0.0"
  }
}
